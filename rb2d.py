from godot import exposed
from godot.bindings import *


@exposed
class rigidbody2d(RigidBody2D):
#    def _ready(self):
#        self.linear_velocity = Vector2(500, 0)
#
#    def _process(self, delta):
#        if self.position.x < 0:
#            self.position.x = 0
#            self.linear_velocity = Vector2(500, 0)
#
#        if self.position.x > self.get_viewport().size.x:
#            self.position.x = self.get_viewport().size.x
#            self.linear_velocity = Vector2(-500, 0)
    pass
